# Security

Do not commit real Tableau workbook exports, site IDs, datasource IDs, source queries, customer data, row-level security mappings, access tokens, credentials, screenshots, or production dashboard evidence.

Use synthetic metric metadata only for public portfolio proof.
